# Core framework
Django>=4.2,<5.0

# Database (MySQL example)
mysqlclient>=2.2.0

# Environment variables management
python-dotenv>=1.0.0

# Asynchronous support (if using async views or channels)
asgiref>=3.8.0

# Security & encryption
bcrypt>=4.0.1
cryptography>=41.0.0

# REST framework (optional, but common in messaging apps)
djangorestframework>=3.15.0

# Messaging / WebSocket support (optional)
channels>=4.0.0
channels-redis>=4.2.0

# Gunicorn for production server
gunicorn>=21.2.0

# Misc utilities
requests>=2.31.0

stage('Install Dependencies') {
    steps {
        sh '''
        apt-get update
        apt-get install -y python3-pip python3-venv
        python3 -m venv venv
        . venv/bin/activate
        pip install -r messaging_app/requirements.txt
        '''
    }
}


